return {
  "Isrothy/neominimap.nvim",
  enabled = false,
  opts = {
    layout = "split",
    split = {
      minimap_width = 10,
      fix_width = true,
      direction = "right",
      close_if_last_window = true,
      -- persist = false,
    },
  },
}
